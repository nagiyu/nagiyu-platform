---

description: "Task list for Codec Converter feature (generated by Spec Kit)
---

# Tasks: Codec Converter (コードックコンバータ)

**Feature フォルダ**: `specs/002-add-codec-converter/`
**入力**: `plan.md`, `spec.md`, `data-model.md`, `contracts/`, `quickstart.md`

## 概要
このドキュメントは `specs/002-add-codec-converter` の設計に基づき、実装タスクをユーザーストーリ別に分割した実行可能なタスクリストです。すべてのタスクはチェックリスト形式で、各タスクに実行可能なファイルパスを含みます。成果物は日本語で記載します。

## フォーマット注記
- 各タスクは必ず次の形式で記載します:
    - [ ] T### [P?] [US?] 説明（ファイルパス）
- ` [P] ` は並列実行可能なタスクを示します（別ファイルで競合しないこと）
- ` [US1]`, ` [US2]`, ` [US3]` は対応するユーザーストーリを示します（ユーザーストーリ段のみ必須）

---

## Phase 1: Setup（セットアップ）

- [ ] T001 `services/codec-converter/package.json` に Next.js ベースのプロジェクト初期化（`services/codec-converter`）
- [ ] T002 `services/codec-converter-worker/Dockerfile` にワーカー用 Dockerfile の作成（FFmpeg ベースイメージのベース）
- [ ] T003 `infra/codec-converter/template.yaml` に CloudFormation スケルトンを追加（S3, DynamoDB, Batch, IAM, ECR のプレースホルダ）
- [ ] T004 [P] `services/codec-converter/.eslintrc.js` と `services/codec-converter/.prettierrc` の追加（lint/format 設定）
- [ ] T005 [P] `services/codec-converter/.github/workflows/ci.yml` の雛形作成（テスト/ビルド/コンテナビルドのワークフロー）

---

## Phase 2: Foundational（基盤）

- [ ] T006 `infra/codec-converter/dynamodb/` に DynamoDB テーブル定義を作成（`codec-converter-jobs-{env}`、TTL カラム `expiresAt` を含む）
- [ ] T007 `infra/codec-converter/s3/` に S3 バケット定義を作成（`uploads/`, `outputs/` バケット、SSE-S3 設定）
- [ ] T008 `infra/codec-converter/iam/` に最低限の IAM ロール（Batch 実行、S3/DynamoDB アクセス）を追加
- [ ] T009 `infra/codec-converter/batch/` に AWS Batch ジョブ定義とキュー/コンピュート環境のスケルトンを追加
- [ ] T010 `infra/codec-converter/ecr/` に ECR リポジトリの CloudFormation 定義を追加
- [ ] T011 [P] `libs/codec-converter-common/package.json` と `libs/codec-converter-common/src/index.ts` に共通ライブラリの雛形を追加（S3/DB ヘルパ等）
- [ ] T012 `services/codec-converter/.env.example` に環境変数テンプレートを作成（DYNAMO_TABLE, S3_BUCKETS, AWS_REGION 等）

---

## Phase 3: User Story 1 - 動画のアップロードと変換 (Priority: P1) 🎯 MVP

**Goal**: ブラウザから MP4 をアップロードし、指定コーデック（H.264/VP9/AV1）へ変換し、ダウンロードできる状態を実装する。

**Independent Test**: ブラウザから 100MB 未満の MP4 をアップロードして H.264 に変換し、変換完了後にダウンロードできること。

### 実装タスク

- [ ] T013 [US1] `services/codec-converter/src/lib/models/job.ts` に `Job` モデル（UUID, status, input S3 path, output S3 path, codec, createdAt, updatedAt, expiresAt, errorMessage）を実装
- [ ] T014 [P] [US1] `services/codec-converter/src/lib/s3.ts` に S3 Presigned URL 生成ユーティリティを実装（アップロード用/ダウンロード用）
- [ ] T015 [US1] `services/codec-converter/src/pages/api/jobs/index.ts` に `POST /api/jobs`（ジョブ作成 → DynamoDB 登録 → ジョブID返却）を実装
- [ ] T016 [US1] `services/codec-converter/src/pages/api/jobs/[jobId].ts` に `GET /api/jobs/{jobId}`（ジョブステータス取得）を実装
- [ ] T017 [US1] `services/codec-converter/src/pages/api/jobs/[jobId]/submit.ts` に `POST /api/jobs/{jobId}/submit`（ジョブを Batch キューへ登録するトリガー）を実装
- [ ] T018 [US1] `services/codec-converter/src/pages/index.tsx` にアップロード UI とコーデック選択 UI を実装（フロント側で拡張子・MIME・サイズ(<=500MB) を検証）
- [ ] T019 [P] [US1] `services/codec-converter-worker/Dockerfile` と `services/codec-converter-worker/entrypoint.sh` にワーカーイメージのエントリ実装（S3 ダウンロード → FFmpeg 実行 → S3 アップロード → DynamoDB 更新）
- [ ] T020 [US1] `services/codec-converter-worker/src/worker.js` にワーカーハンドラー（ジョブの取り出しと変換ロジックのラッパ）を実装
- [ ] T021 [US1] `services/codec-converter/src/lib/db.ts` に DynamoDB アクセスラッパ（getJob, putJob, updateJobStatus）。`putJob` 実行時に `expiresAt` を `createdAt + 86400`（24時間後）としてセットすることを明記
- [ ] T022 [P] [US1] `services/codec-converter/src/lib/logger.ts` にログ/エラーハンドリングユーティリティを追加
- [ ] T023 [US1] `services/codec-converter/jest.config.js` と `services/codec-converter/tests/api.test.ts` に最小限の API 単体テストスケルトンを追加（オプション）

**Checkpoint**: 上記が完了すれば、ローカル環境またはステージングでジョブ作成→Batch 実行→S3 出力の一連が検証可能

---

## Phase 4: User Story 2 - ジョブ管理と進捗確認 (Priority: P2)

**Goal**: ブラウザでローカルに保存したジョブ ID の一覧から各ジョブのステータスを取得して表示する。

**Independent Test**: ジョブ作成後、ローカルストレージに保存した ID をページ再読み込みして API からステータスが取得できること。

- [ ] T024 [US2] `services/codec-converter/src/lib/localJobs.ts` にローカルストレージ管理ユーティリティ（保存/取得/削除）を実装
- [ ] T025 [US2] `services/codec-converter/src/components/JobList.tsx` にジョブ一覧コンポーネントを実装（API からステータスを取得して表示）
- [ ] T026 [US2] `services/codec-converter/src/pages/jobs.tsx` にジョブ一覧ページを実装（`JobList` を組み込み）
- [ ] T027 [US2] `services/codec-converter/src/pages/api/jobs/list.ts` に複数ジョブのバッチ取得 API（クライアントから受け取ったジョブ ID リストで取得）を実装

---

## Phase 5: User Story 3 - ダウンロードと保持 (Priority: P3)

**Goal**: 変換完了後に Presigned URL を発行してダウンロード可能にし、出力ファイルは 24 時間後に削除する。

**Independent Test**: 変換完了後にダウンロード用 Presigned URL を生成して 24 時間以内にダウンロードできること。

- [ ] T028 [US3] `services/codec-converter/src/pages/api/jobs/[jobId]/download.ts` にダウンロード用 Presigned URL 発行エンドポイントを実装
- [ ] T029 [US3] `infra/codec-converter/s3-lifecycle.yaml` に S3 ライフサイクルルール（オブジェクト削除/移行）を追加（補助措置）

---

## Phase N: Polish & Cross-Cutting Concerns

- [ ] T030 `docs/apps/codec-converter/README.md` にセットアップ手順とクイックスタートを記載（`specs/002-add-codec-converter/quickstart.md` の内容参照）
- [ ] T031 `infra/codec-converter/README.md` にデプロイ手順と依存関係をまとめる
- [ ] T032 [P] `.github/workflows/deploy-codec-converter.yml` に ECR ビルド→ECR push→CloudFormation deploy のワークフロー雛形を追加
- [ ] T033 `services/codec-converter/SECURITY.md` に Phase 1 のセキュリティ考慮事項（匿名利用、S3 バケットポリシー、最大ファイルサイズ）を記載

---

## 依存関係 & 実行順序

- Setup（Phase 1） を完了 → Foundational（Phase 2） を完了 → 各ユーザーストーリ（Phase 3+）を実装。
- Foundational はユーザーストーリ実装の前に完了させる（DynamoDB テーブル、S3 バケット、IAM 等）。
- ユーザーストーリ間は基本的に独立して実装可能（並列化可能）。

## 並列実行の例

- `T004`, `T005`, `T011` のような設定／ライブラリ作成は並列化可能
- `T014` (S3 presigned) と `T022` (logger) は複数ストーリで使えるため先に作業しておくと効率的
- ワーカーイメージ作成（`T019`）と API 実装（`T015`, `T016`, `T017`）は独立して進められる（ただし統合テスト時に結合する）

## 実装戦略（推奨）

1. MVP: `User Story 1` のみを最初に完了させる（T013〜T023）
2. Foundation を整備（T006〜T012）を並行して実装しておくとデプロイが容易
3. US1 完了後、US2 → US3 の順に追加していく（各ストーリは独立テストが可能）

---

## 生成結果のサマリ

- 総タスク数: 33（T001〜T033）
- User Story 別タスク数:
    - US1 (P1): 11 タスク（T013〜T023）
    - US2 (P2): 4 タスク（T024〜T027）
    - US3 (P3): 2 タスク（T028〜T029）
    - Setup/Foundational: 12 タスク（T001〜T012）
    - Polish: 4 タスク（T030〜T033）

- 並列化の候補: T004, T005, T011, T014, T019, T021, T032 など（各タスクの `[P]` を参照）
- MVP 推奨スコープ: `User Story 1` のみ（まずは T013〜T023 を完了し、動作確認を行う）

---

## 形式バリデーション
- すべてのタスクは `- [ ] T### [P?] [US?] 説明（ファイルパス）` のチェックリスト形式に従っています。

---

Generated by Spec Kit (日本語出力)
