# テスト環境用の環境変数
# E2E テストで認証をスキップするための設定

NODE_ENV=test

# 認証スキップフラグ
SKIP_AUTH_CHECK=true

# テストユーザー情報
TEST_USER_EMAIL=test@example.com
TEST_USER_ROLES=admin

# NextAuth の必須設定（E2E テスト用）
NEXTAUTH_SECRET=test-secret-key-for-e2e-tests-must-be-at-least-32-characters-long
NEXTAUTH_URL=http://localhost:3000

# Google OAuth は認証スキップ時は不要だが、警告を消すため空文字を設定
GOOGLE_CLIENT_ID=test-client-id
GOOGLE_CLIENT_SECRET=test-client-secret
