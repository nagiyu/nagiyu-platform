# AWS Batch ローカルテスト用環境変数
# 実際の値は .env.local に記述してください（.env.local は .gitignore に含まれています）

# DynamoDB テーブル名
DYNAMODB_TABLE_NAME=dev-niconico-mylist-assistant

# AWS リージョン
AWS_REGION=us-east-1

# 暗号化設定
# パスワード復号化に使用する Secrets Manager のシークレット名
ENCRYPTION_SECRET_NAME=niconico-mylist-assistant/shared-secret-key

# Node Environment
NODE_ENV=development

# ジョブパラメータ（環境変数経由で渡す場合）
# 注: 本番環境では AWS Batch のジョブパラメータとして渡されます
USER_ID=test-user-id
NICONICO_EMAIL=your-test-account@example.com
ENCRYPTED_PASSWORD={"ciphertext":"...","iv":"...","authTag":"..."}
MYLIST_NAME=テストマイリスト
VIDEO_IDS=["sm9","sm10"]

# 注意事項:
# - ENCRYPTED_PASSWORD は暗号化済みのJSON文字列です（core/crypto.ts で暗号化）
# - テスト用アカウントは、本番環境とは別のアカウントを使用してください
# - .env.local ファイルはバージョン管理に含めないでください
# - ローカルテストでは AWS 認証情報が設定されている必要があります
