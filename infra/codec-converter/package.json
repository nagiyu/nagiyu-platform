{
  "name": "codec-converter",
  "version": "0.1.0",
  "bin": {
    "codec-converter": "bin/codec-converter.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "bootstrap": "cdk bootstrap --require-approval never",
    "deploy": "cdk deploy --require-approval never",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "aws-cdk": "2.1100.1"
  },
  "dependencies": {
    "@nagiyu/infra-common": "file:../common",
    "aws-cdk-lib": "2.232.1",
    "constructs": "^10.0.0"
  }
}
