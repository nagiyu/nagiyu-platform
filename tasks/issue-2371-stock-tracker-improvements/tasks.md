# Stock Tracker 改善 - 実装タスク（Issue #2371）

<!--
    このドキュメントは開発時のみ使用します。
    開発完了後に tasks/issue-2371-stock-tracker-improvements/ ディレクトリごと削除します。

    参照ドキュメント:
    - tasks/issue-2371-stock-tracker-improvements/requirements.md — 受け入れ条件・ユースケース
    - tasks/issue-2371-stock-tracker-improvements/design.md — 変更モジュール・コンポーネント設計
-->

## Phase 1: バッチ変更（依存なし、単独で完結）

### 1-1. 一時アラート失効処理の無効化→削除変更

- [ ] T001: `batch/src/temporary-alert-expiry.ts` を開き、`alertRepo.update(userId, alertId, { Enabled: false })` の呼び出し箇所を `alertRepo.delete(userId, alertId)` に変更する（依存: なし）
- [ ] T002: `batch/tests/unit/temporary-alert-expiry.test.ts` を更新し、モックの検証対象を `update` の呼び出し確認から `delete` の呼び出し確認に変更する（依存: T001）
- [ ] T003: バッチの単体テストが通過することを確認する（依存: T002）

### 1-2. AI 解析 Web 検索強制化

- [ ] T004: `batch/src/lib/openai-client.ts` の OpenAI Responses API 呼び出しオプションに `tool_choice: 'required'` を追加する（依存: なし）
- [ ] T005: 同ファイルのプロンプト内 `relatedMarketTrend` フィールドの説明文を「必ずWeb検索を利用して最新情報を取得すること」に変更する（依存: T004）
- [ ] T006: 可能であれば、決算発表・重要経済指標など直近のニュースも根拠として含めるよう指示をプロンプトに追加する（依存: T005）
- [ ] T007: `batch/tests/unit/openai-client.test.ts` を更新し、呼び出しオプションに `tool_choice: 'required'` が含まれることを検証するアサーションを追加する（依存: T004）
- [ ] T008: AI 解析関連の単体テストが通過することを確認する（依存: T007）

---

## Phase 2: Web フロント変更 — バグ修正（依存なし、単独で完結）

### 2-1. サマリー詳細ダイアログ モバイル横幅修正

- [ ] T009: モバイル幅（375px 程度）でサマリー詳細ダイアログを表示し、横幅を超えている要素を特定する（依存: なし）
- [ ] T010: MUI Dialog の `PaperProps` または `sx` prop でモバイル幅が `100vw` を超えないよう設定する（依存: T009）
- [ ] T011: ダイアログ内のテーブル・横長コンテンツに `overflow-x: auto` を設定する（依存: T009）
- [ ] T012: ダイアログ内の長文テキストに `wordBreak: 'break-word'` を設定する（依存: T009）
- [ ] T013: モバイル幅でダイアログが画面幅に収まり横スクロールが発生しないことを確認する（依存: T010, T011, T012）

---

## Phase 3: Web フロント変更 — トップ画面サマリーパネル刷新（依存: なし）

### 3-1. 日次サマリーパネルの表示項目変更

- [ ] T014: トップ画面の日次サマリーパネルコンポーネントを特定し、現在の OHLCV 表示ロジックを確認する（依存: なし）
- [ ] T015: 投資判断・買いシグナル件数・売りシグナル件数・AI 判定・更新時間を表示するようパネルの表示内容を変更する（依存: T014）
- [ ] T016: サポートレベル・レジスタンスレベルのデータが存在する場合に表示行を出す条件付き表示を実装する（依存: T015）
- [ ] T017: サマリーデータが存在しない場合・AI 判定未生成の場合の空状態表示を実装する（依存: T015）

### 3-2. 詳細ダイアログ表示ボタンの追加

- [ ] T018: SCR-004 で使用しているサマリー詳細ダイアログコンポーネントのパスと props インターフェースを確認する（依存: なし）
- [ ] T019: 日次サマリーパネルに「詳細」ボタンを追加する（依存: T018）
- [ ] T020: ボタン押下でサマリー詳細ダイアログが開き、現在選択中のティッカーのサマリーデータが表示されることを確認する（依存: T015, T019）

---

## Phase 4: Web フロント変更 — トップ画面 CRUD 追加（依存: なし、Phase 3 と並列実行可能）

### 4-1. 保有株式パネルへの CRUD 追加

- [ ] T021: SCR-002 で使用している保有株式作成・編集・削除ダイアログコンポーネントのパスと props インターフェースを確認する（依存: なし）
- [ ] T022: 保有株式パネルに「追加」ボタンを追加し、クリックで作成ダイアログが開くようにする（取引所・ティッカーは選択中の値をデフォルト設定）（依存: T021）
- [ ] T023: 保有株式が存在する行に「編集」ボタンを追加し、クリックで編集ダイアログが開くようにする（依存: T021）
- [ ] T024: 保有株式が存在する行に「削除」ボタンを追加し、確認後に削除されるようにする（依存: T021）
- [ ] T025: CRUD 操作完了後に保有株式パネルのデータが再取得・再表示されることを確認する（依存: T022, T023, T024）
- [ ] T026: 削除後にチャートの保有平均価格オーバーレイラインが更新されることを確認する（依存: T025）

### 4-2. アラートパネルへの CRUD 追加

- [ ] T027: SCR-003 で使用しているアラート作成・編集・削除ダイアログコンポーネントのパスと props インターフェースを確認する（依存: なし）
- [ ] T028: アラートパネルに「追加」ボタンを追加し、クリックで作成ダイアログが開くようにする（取引所・ティッカーは選択中の値をデフォルト設定）（依存: T027）
- [ ] T029: アラートが存在する行に「編集」ボタンを追加し、クリックで編集ダイアログが開くようにする（依存: T027）
- [ ] T030: アラートが存在する行に「削除」ボタンを追加し、確認後に削除されるようにする（依存: T027）
- [ ] T031: CRUD 操作完了後にアラートパネルのデータが再取得・再表示されることを確認する（依存: T028, T029, T030）
- [ ] T032: 削除後にチャートのアラートしきい値オーバーレイラインが更新されることを確認する（依存: T031）

---

## Phase 5: 結合確認

- [ ] T033: トップ画面でサマリーパネル・保有株式パネル・アラートパネルの各変更が意図通りに動作することを手動確認する（依存: Phase 3〜4 完了）
- [ ] T034: モバイル幅（375px）でトップ画面および SCR-004 の詳細ダイアログが横幅内に収まることを確認する（依存: T013, T020）

---

## 完了チェック

- [ ] `requirements.md` の受け入れ条件（FR-2371-01 〜 FR-2371-09）をすべて満たしている
- [ ] `design.md` の「docs/ への移行メモ」を処理した
- [ ] `docs/services/stock-tracker/requirements.md` を更新した
- [ ] `docs/services/stock-tracker/external-design.md` を更新した
- [ ] `tasks/issue-2371-stock-tracker-improvements/` ディレクトリを削除した
