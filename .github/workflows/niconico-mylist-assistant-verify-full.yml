name: Niconico Mylist Assistant - Full Verification

on:
  pull_request:
    branches:
      - develop
    paths:
      - 'services/niconico-mylist-assistant/**'
      - 'libs/**'
      - 'infra/niconico-mylist-assistant/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/niconico-mylist-assistant-verify-full.yml'

jobs:
  nextjs-build:
    name: Next.js Build Verification
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: './package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Build shared libraries
        run: |
          npm run build --workspace @nagiyu/common
          npm run build --workspace @nagiyu/browser
          npm run build --workspace @nagiyu/ui

      - name: Build core package
        run: npm run build --workspace @nagiyu/niconico-mylist-assistant-core

      - name: Build Next.js application
        run: npm run build --workspace @nagiyu/niconico-mylist-assistant-web

      - name: Build batch package
        run: npm run build --workspace @nagiyu/niconico-mylist-assistant-batch

  docker-build-web:
    name: Docker Build Verification (web)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build web Docker image
        run: docker build -t niconico-mylist-assistant-web-verify-full-test -f services/niconico-mylist-assistant/web/Dockerfile .

  docker-build-batch:
    name: Docker Build Verification (batch)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build batch Docker image
        run: docker build -t niconico-mylist-assistant-batch-verify-full-test -f services/niconico-mylist-assistant/batch/Dockerfile .

  test:
    name: Run Unit Tests (core)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: './package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm run test --workspace @nagiyu/niconico-mylist-assistant-core

  coverage:
    name: Test Coverage Check (80% required)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: './package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Run coverage check
        run: npm run test:coverage --workspace @nagiyu/niconico-mylist-assistant-core

  e2e-test:
    name: Run E2E Tests (all devices)
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: './package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Build shared libraries
        run: |
          npm run build --workspace @nagiyu/common
          npm run build --workspace @nagiyu/browser
          npm run build --workspace @nagiyu/ui

      - name: Build core package
        run: npm run build --workspace @nagiyu/niconico-mylist-assistant-core

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps
        working-directory: ./services/niconico-mylist-assistant/web

      - name: Run Playwright tests (all devices)
        env:
          CI: true
        run: |
          npm run test:e2e --workspace @nagiyu/niconico-mylist-assistant-web
          if [ $? -ne 0 ]; then
            echo "E2E tests failed"
            exit 1
          fi

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report-full
          path: services/niconico-mylist-assistant/web/playwright-report/
          retention-days: 30

      - name: Upload test artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results-full
          path: services/niconico-mylist-assistant/web/test-results/
          retention-days: 30

  integration-test:
    name: Batch Integration Test
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: './package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Build shared libraries
        run: |
          npm run build --workspace @nagiyu/common
          npm run build --workspace @nagiyu/browser

      - name: Build core package
        run: npm run build --workspace @nagiyu/niconico-mylist-assistant-core

      - name: Build batch package
        run: npm run build --workspace @nagiyu/niconico-mylist-assistant-batch

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps chromium
        working-directory: ./services/niconico-mylist-assistant/batch

      - name: Run batch integration tests
        env:
          CI: true
          TEST_ACCOUNT_EMAIL: ${{ secrets.NICONICO_TEST_ACCOUNT_EMAIL }}
          TEST_ACCOUNT_PASSWORD: ${{ secrets.NICONICO_TEST_ACCOUNT_PASSWORD }}
        run: npm run test --workspace @nagiyu/niconico-mylist-assistant-batch

  lint:
    name: Lint Code
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: './package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Run lint (core)
        run: npm run lint --workspace @nagiyu/niconico-mylist-assistant-core

      - name: Run lint (web)
        run: npm run lint --workspace @nagiyu/niconico-mylist-assistant-web

      - name: Run lint (batch)
        run: npm run lint --workspace @nagiyu/niconico-mylist-assistant-batch

  format-check:
    name: Format Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: './package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Check formatting (web)
        run: npm run format:check --workspace @nagiyu/niconico-mylist-assistant-web

  report:
    name: Report Results to PR
    runs-on: ubuntu-latest
    if: always()
    needs: [nextjs-build, docker-build-web, docker-build-batch, test, coverage, e2e-test, integration-test, lint, format-check]

    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Create PR Comment
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const results = {
              'Next.js Build': '${{ needs.nextjs-build.result }}',
              'Docker Build (web)': '${{ needs.docker-build-web.result }}',
              'Docker Build (batch)': '${{ needs.docker-build-batch.result }}',
              'Unit Tests': '${{ needs.test.result }}',
              'Coverage Check': '${{ needs.coverage.result }}',
              'E2E Tests (Full)': '${{ needs.e2e-test.result }}',
              'Integration Test': '${{ needs.integration-test.result }}',
              'Lint': '${{ needs.lint.result }}',
              'Format Check': '${{ needs.format-check.result }}'
            };

            const statusEmoji = {
              'success': '✅',
              'failure': '❌',
              'cancelled': '⚠️',
              'skipped': '⏭️'
            };

            const allSuccess = Object.values(results).every(r => r === 'success');
            const header = allSuccess
              ? '## ✅ All Full Checks Passed'
              : '## ❌ Some Full Checks Failed';

            let body = `${header}\n\n`;
            body += '**Full Verification (all devices, coverage check)**\n\n';
            body += '| Job | Status |\n';
            body += '|-----|--------|\n';

            for (const [job, result] of Object.entries(results)) {
              const emoji = statusEmoji[result] || '❓';
              body += `| ${job} | ${emoji} ${result} |\n`;
            }

            body += `\n[View workflow run](${context.payload.repository.html_url}/actions/runs/${context.runId})`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });
